<?xml version="1.0" encoding="utf-8"?>
<!-- No package filter on purpose: the service must also know when the driver
     leaves Uber Driver so it can remove the result card. The code itself only
     reads the screen of com.ubercab.driver. -->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged|typeWindowContentChanged|typeWindowsChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:accessibilityFlags="flagRetrieveInteractiveWindows"
    android:canRetrieveWindowContent="true"
    android:canTakeScreenshot="true"
    android:description="@string/accessibility_service_description"
    android:notificationTimeout="80"
    android:settingsActivity="com.superdriver.app.MainActivity" />
